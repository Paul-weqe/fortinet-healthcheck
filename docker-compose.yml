version: "3.8"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      - .:/code

  db:
    image: postgres:13.2
    volumes:
      - ../data:/var/lib/postgresql/data
    env_file:
      - .env